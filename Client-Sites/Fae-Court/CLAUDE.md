# Fae Court — Client Site

Client prospect. Reference media only at this stage — no site built yet. Generated through the Launch Local pipeline.

## Location
`C:\Users\Woodl\Documents\AI_Projects\Launch Local\Client-Sites\Fae-Court\`

## Status
Prospect / reference media only. No Firebase project, no GitHub repo, no deploy pipeline — set those up via the standard Launch Local flow once the client signs.

## Roadmap
_Auto-maintained by the dashboard refresh (daily noon + Claude Code session-end hook). Top 3 open items surface as to-dos in the AI_Projects dashboard. `[ ]` = open, `[x]` = done. Prefer engaging/build-style items over pure cleanup. Keep item text stable so check-off state persists in the dashboard. The loop may mark items done based on recent commits, add inferred new items, and reorder — manual edits always win._

- [ ] _(This list will auto-populate on the next dashboard refresh based on recent activity in this folder. Currently a prospect — pitch and close before building further.)_

## Rules for Claude
1. Read this file first, then identify which files are affected and state why
2. Use `filesystem:edit_file` for targeted changes; `filesystem:write_file` only for new files or full rewrites — state which
3. Apply all changes in one pass without re-reading
4. On finish: confirm complete or provide a ready-to-paste handoff prompt
5. Update this file if status or scope changes
